import type { ReactNode } from 'react'

// Placeholder entry for the Team To-Do feature. Frontend-engineer replaces this
// with the authenticated shell + routes (sign-in, teams, lists, tasks) built to
// the designer's mocks and consuming the generated API client. Kept minimal so
// the flag seam and feature barrel exist from S0 without pre-empting the UI work.
export function TeamTodoApp(): ReactNode {
  return <main>Team To-Do (coming soon)</main>
}
